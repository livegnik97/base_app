name: base_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0-290.4.beta

dependencies:
  animate_do: ^4.2.0
  auto_size_text: ^3.0.0
  cached_network_image: ^3.4.1
  connectivity_plus: ^7.0.0
  dio: ^5.9.1
  extended_image: ^10.0.1
  firebase_core: ^4.4.0
  firebase_messaging: ^16.1.1
  firebase_messaging_platform_interface: ^4.7.6
  flutter:
    sdk: flutter
  flutter_debouncer: ^2.1.1
  flutter_local_notifications: ^20.0.0
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  flutter_typeahead: ^5.2.0
  formz: ^0.8.0
  go_router: ^17.1.0
  image_picker: ^1.2.1
  intl: ^0.20.2
  json_view: ^0.4.2
  loading_animation_widget: ^1.3.0
  permission_handler: ^12.0.1
  shared_preferences: ^2.5.4
  socket_io_client: ^3.1.4
  url_launcher: ^6.3.2
  vibration: ^3.1.6

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# dart run flutter_launcher_icons
# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: true
#   image_path: "assets/imagen/tiky-tiky-icon.png"
#   min_sdk_android: 21

# dart run android_notification_icons
# android_notification_icons:
#   image_path: "assets/imagen/tiky-tiky-icon-notification.png"
#   icon_name: 'ic_notification' # optional

flutter:
  # para el idioma hay que agregar esto
  generate: true

  uses-material-design: true
  # assets:
  #   - assets/imagen/
